# Машинно-специфичное и служебное — не должно попадать в git, у каждого разработчика/машины своё.
config.json
state.json
autosync.log
autosync_crash.log
__pycache__/
*.pyc

# Ветка, с которой сверяется самообновление (ЗАПУСК_САМОЙ_ПРОГРАММЫ, см. self_update.py) — у
# рабочей копии "Stable", у копии для разработки "alpha/Mzs7". Файла нет — по умолчанию "Stable".
update_branch.txt
